{% extends 'base.html.twig' %}

{% block body %}
    <h1>RankingElementRules</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rankingElementRule.id }}</td>
            </tr>
            <tr>
                <th>Rules</th>
                <td>{{ rankingElementRule.rules }}</td>
            </tr>
            <tr>
                <th>Elementdetailsid</th>
                <td>{{ rankingElementRule.elementDetailsId }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rankingelementrules_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('rankingelementrules_edit', { 'id': rankingElementRule.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
