{% extends 'base.html.twig' %}

{% block body %}
    <h1>RankingKpiRules</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rankingKpiRule.id }}</td>
            </tr>
            <tr>
                <th>Rules</th>
                <td>{{ rankingKpiRule.rules }}</td>
            </tr>
            <tr>
                <th>Kpidetailsid</th>
                <td>{{ rankingKpiRule.kpiDetailsId }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('rankingkpirules_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('rankingkpirules_edit', { 'id': rankingKpiRule.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
