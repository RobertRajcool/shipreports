{% extends 'base.html.twig' %}
{#{% block title %}Star Shipping Application{% endblock %}#}

{% block body %}
    {% block javascripts %}

        {% javascripts '@InitialShippingBundle/Resources/public/js/*' %}
        <script src="{{ asset_url }}"></script>
        {% endjavascripts %}



    {% endblock %}
    <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
        <div class="page-header">
            <h2>Kpi Values List</h2>
            <a href="{{ path('readingkpivalues_new') }}">Add Kpi Value</a>
        </div>
        <div class="table-responsive">
            <table class="table table-striped" >
                <thead>
                <tr>

                    <th>Ship Name</th>
                    <th>Kpi Name</th>
                    <th>Element Name</th>
                    <th>Value</th>
                    <th>Monthdetail</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for readingKpiValue in readingKpiValues %}
                    <tr>

                        <td>{{ readingKpiValue.shipDetailsId }}</td>
                        <td>{{ readingKpiValue.kpiDetailsId }}</td>
                        <td>{{ readingKpiValue.elementDetailsId }}</td>
                        <td>{{ readingKpiValue.value }}</td>
                        <td>
                            {#{% if readingKpiValue.monthdetail %}#}
                                {{ readingKpiValue.monthdetail|date('Y-m-d') }}
                            {#{% endif %}#}</td>
                        <td>

                            <a href="{{ path('readingkpivalues_show', { 'id': readingKpiValue.id }) }}">show</a>

                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
        {#{% if last_page > 1 %}
            <div class="pagination">
                <a href="{{ path('readingkpivalues_index', { 'page': 1 }) }}">First page </a>

                <a href="{{ path('readingkpivalues_index', {  'page': previous_page }) }}">Previous page </a>

                {% for page in 1..last_page %}
                    {% if page == current_page %}
                        {{ page }}
                    {% else %}
                        <a href="{{ path('readingkpivalues_index', {  'page': page }) }}">{{ page }}</a>
                    {% endif %}
                {% endfor %}

                <a href="{{ path('readingkpivalues_index', {  'page': next_page }) }}">Next page
                </a>

                <a href="{{ path('readingkpivalues_index', {  'page': last_page }) }}">Last page
                </a>
            </div>
        {% endif %}#}

        {#<div class="pagination_desc">
            <strong>{{ total_jobs }}</strong> Total Records

            {% if last_page > 1 %}
                - page <strong>{{ current_page }}/{{ last_page }}</strong>
            {% endif %}
        </div>#}

        <div class="bs-example">
            <ul class="pagination">
                <li class="disabled"><a href="{{ path('readingkpivalues_index', {  'page': previous_page }) }}">Prev</a></li>
                <li class="active"><a href="{{ path('readingkpivalues_index', { 'page': 1 }) }}">1</a></li>
                {% for page in 1..last_page %}
                    {% if page == current_page %}
                        {{ page }}
                    {% else %}
                <li>  <a href="{{ path('readingkpivalues_index', {  'page': page }) }}">{{ page }}</a></li>
                    {% endif %}
                {% endfor %}

                <li> <a href="{{ path('readingkpivalues_index', {  'page': next_page }) }}">Next</a></li>
                <li> <a href="{{ path('readingkpivalues_index', {  'page': last_page }) }}">Last</a></li>
            </ul>
        </div>
    </div>

{% endblock %}
